"use client";
import {Archive,BookOpen,Folder,Plus} from 'lucide-react';
import type {SpecNavigation} from '@/lib/specifications';
export default function SpecificationNavigation({navigation,value,onChange,onCreate}:{navigation:SpecNavigation|null;value:string;onChange:(id:string)=>void;onCreate:()=>void}){
 const groups=navigation?.groups||[];
 return <div className="sidebar-photo-links sidebar-specification-links"><div className="sidebar-photo-group-heading"><h2>Specification groups</h2>{navigation?.canEdit&&<button className="icon-button" aria-label="Create specification group" onClick={onCreate}><Plus size={17}/></button>}</div><nav aria-label="Specification groups"><button className={'project-nav-entry '+(value==='all'?'active':'')} aria-pressed={value==='all'} onClick={()=>onChange('all')}><BookOpen size={17}/><span>All specifications</span><small>{groups.reduce((n,g)=>n+g.count,navigation?.ungrouped||0)}</small></button>{groups.map(g=><button key={g.id} className={'project-nav-entry '+(value===g.id?'active':'')} aria-pressed={value===g.id} onClick={()=>onChange(g.id)}><Folder size={17}/><span>{g.name}</span><small>{g.count}</small></button>)}<button className={'project-nav-entry '+(value==='ungrouped'?'active':'')} aria-pressed={value==='ungrouped'} onClick={()=>onChange('ungrouped')}><Folder size={17}/><span>Ungrouped</span><small>{navigation?.ungrouped||0}</small></button>{!!navigation?.archived&&<button className={'project-nav-entry '+(value==='archived'?'active':'')} aria-pressed={value==='archived'} onClick={()=>onChange('archived')}><Archive size={17}/><span>Archived</span><small>{navigation.archived}</small></button>}</nav></div>
}
